// activationg strict mode
// to activate strict mode, write the code:
// "use strict" at the very fist line of the script file
// otherwise it won't work for whole file.
// "use strict" has to be the first line of code that will run
// having comment above it will have no problem,just like these ones.
"use strict";

// strict mode helps us to find error in our script file.
let hasDriverLicense = false;
const passTest = true;
if (passTest) hasDriverLicense = true;
if (hasDriverLicense) {
  console.log("I can drive");
}

// const private = 25;
// const if = 8;
